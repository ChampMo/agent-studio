"""Tools: what an agent can actually do to this machine (§16).

Nothing here is imported for its side effects. The registry is built in
`registry.py`, path safety lives in `paths.py`, and each tool is a function that
takes a resolved context rather than reading anything global.
"""
